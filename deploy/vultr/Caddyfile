{$API_DOMAIN} {
	encode zstd gzip
	log

	# 현재 API는 X-User-Id를 신뢰하므로 사용자 인증을 완성할 때까지 개발팀만 접근해요.
	# 상태 확인과 브라우저 사전 요청은 데이터를 바꾸지 않는 공용 경로예요.
	@protected {
		not path /v1/health
		not method OPTIONS
	}
	basic_auth @protected {
		{$COLLAB_USER} {$COLLAB_PASSWORD_HASH}
	}
	reverse_proxy api:8000
}
